import React, { Suspense, useRef } from 'react';
import styles from './index.scss';

const HomeScreen = () => {
  return (
    <div>
      <p>Home Screen</p>
    </div>
  );
};

export default HomeScreen;
