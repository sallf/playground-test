// This is a sample test to ensure the testing setup is working
// expected to be removed in actual development env.
describe('Sample test', () => {
  test('test for truthy', () => {
    const input = true;
    expect(input).toBe(true);
  });
});
